/**
 * Module: pages/Settings.jsx
 * Purpose: Supports the Settings module and keeps its responsibility isolated by file name.
 */
import SettingsPage from '../features/settings/SettingsPage';

export default SettingsPage;
